<IfModule mod_rewrite.c>
  RewriteEngine On

  # Redirect root to /app
  RewriteRule ^$ /app/ [R=301,L]
  RewriteRule ^index\.html$ /app/ [R=301,L]
</IfModule>

# Disable directory listing
Options -Indexes

# Custom 404 error page
ErrorDocument 404 /404.html